{
    "require": {
       
        "ext-redis": "*"
    },
    "extra": {
        "paas": {
          "php-config": [
            "session.save_handler = redis",
            "session.save_path = 'tcp://127.0.0.1:6379'"
          ]
        }
      }

}
